# Copia este archivo a ".env" (ignorado por git) y completa UNA de las dos vias.

# Via recomendada: token OAuth de larga duracion generado con `claude setup-token`.
CLAUDE_CODE_OAUTH_TOKEN=

# Via alternativa: API key de la consola de Anthropic.
# ANTHROPIC_API_KEY=
